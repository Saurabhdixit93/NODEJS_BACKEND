<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login OTP</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f0f0f0;
        margin: 0;
        padding: 0;
        text-align: center;
      }
      .container {
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        margin: 20px auto;
        max-width: 600px;
        padding: 20px;
      }
      h1 {
        color: #333;
      }
      p {
        color: #555;
      }
      .otp-container {
        background-color: #007bff;
        color: #fff;
        border-radius: 5px;
        padding: 10px;
        font-size: 18px;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Login OTP</h1>
      <p>Your One-Time Password (OTP) for login:</p>
      <div class="otp-container"><%= Otp %></div>
      <p>
        This OTP is valid until <%= otpExpiresAt %>. Please use it to log in.
      </p>
    </div>
  </body>
</html>
